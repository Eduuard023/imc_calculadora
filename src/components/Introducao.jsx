import React from 'react';

const Introducao = ({ nome }) => {
    return (
        <div>
            <h1>Olá, {nome}! Vamos calcular seu IMC.</h1>
        </div>
    );
};

export default Introducao;
